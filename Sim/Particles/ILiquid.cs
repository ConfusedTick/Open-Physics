using System;
using System.Collections.Generic;
using System.Text;

namespace Sim.Particles
{

    // Interface that all liquids should use
    // Does not have any default realization
    // Well, liquid means that its start aggreg state is liquid 
    // and not anything else
    interface ILiquid
    {
    }
}
